<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DeliveryApp.Views.ResMenuPage">
    <ContentPage.Resources>
       
    </ContentPage.Resources>
    <ContentPage.Content>
        <StackLayout>
            <Label Text="hello">
            </Label>
            <CollectionView x:Name="Menu" ItemsSource="{Binding ResList}" >
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <SwipeView>
                            
                            <SwipeView.TopItems>
                                <SwipeItems>
                                    <SwipeItem Text="{Binding Description}"/> <!--cant be bothered now fix later-->
                                </SwipeItems>
                            </SwipeView.TopItems>
                            <Frame HeightRequest="40">
                                <Grid Padding="10">
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="100" />
                                        <RowDefinition Height="100" />
                                        <RowDefinition Height="Auto" />
                                        
                                    </Grid.RowDefinitions>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="100" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>

                                    <Image Grid.Row="0" Grid.Column="0"
                                Source="{Binding Image}"
                                HeightRequest="100"
                                WidthRequest="100" 
                                          />

                               <Label Grid.Column="1" Grid.Row="0"
                               Text="{Binding Name}"
                               FontAttributes="Bold" TextColor="Black" FontSize="Small" />
                                </Grid>

                               
                            </Frame>

                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </StackLayout>
    </ContentPage.Content>
</ContentPage>

    
    
    
    
    
    
    
    