<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DeliveryApp.Views.ResMenuPage">
    <ContentPage.Content>
        <StackLayout>


            <CollectionView x:Name="Menu"   ItemsSource="{Binding ResList}" SelectionMode="Single"
                        SelectionChangedCommand="{Binding SelctionChanged}"
                        SelectionChangedCommandParameter="{Binding Source={x:Reference ResList}, Path=SelectedItem}">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="10" HorizontalOptions="FillAndExpand">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="100" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="100" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <Image Grid.Column="0" Grid.Row="0"
                                   Source="{Binding Image}"/>
                            <Label Grid.Column="0" Grid.Row="1"
                                   Text="{Binding Name}" 
                                   FontAttributes="Bold" FontSize="Large" HorizontalOptions="Center"/>
                            <Label Grid.Column="1" Grid.Row="0" Grid.RowSpan="2"
                                   Text="{Binding Description}" 
                                   FontAttributes="Italic"/>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
            
            
           
            
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
